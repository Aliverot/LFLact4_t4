info:
  name: Seguridad GET
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/videojuegos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
